<%- include("partials/header") %>

    <div class="row row-cols-2 row-cols-md-2 g-4">
        <div class="col">
          <div class="card">
            <img src="/images/<%= tweet.img %>" class="card-img-top" alt="..." />
            <div class="card-body">
              <h5 class="card-title"><%= tweet.username %></h5>
              <p class="card-texts"><%= tweet.comment %></p>
            </div>
          </div>

          <form method="POST" action="/tweet/<%= tweet.id %>?_method=DELETE ">
            <button class="btn btn-outline-primary">Delete</button>
          </form>
  <a href="/tweet">Back to Homepage</a>
        
        </div>
      </div>
    </div>
    <%- include("partials/footer") %>

